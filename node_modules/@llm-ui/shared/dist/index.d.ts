import { MaybeLLMOutputMatch, LLMOutputMatch } from '@llm-ui/react';

declare const regexMatcher: (regex: RegExp) => (llmOutput: string) => MaybeLLMOutputMatch;
declare const regexMatcherGlobal: (regex: RegExp) => (llmOutput: string) => LLMOutputMatch[];

type StartEndChars = {
    startChar: string;
    endChar: string;
};
declare const removeStartEndChars: (str: string, { startChar, endChar }: StartEndChars) => string;

export { regexMatcher, regexMatcherGlobal, removeStartEndChars };
